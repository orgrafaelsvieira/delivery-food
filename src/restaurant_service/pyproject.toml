[tool.poetry]
name = "delivery-food"
version = "0.1.0"
description = ""
authors = []
package-mode = false

[tool.poetry.dependencies]
python = ">=3.11"
fastapi = {extras = ["standard"], version = ">=0.118.0,<0.119.0"}
sqlalchemy = ">=2.0.43,<3.0.0"
redis = ">=6.4.0,<7.0.0"
psycopg = {extras = ["binary", "pool"], version = ">=3.2.10,<4.0.0"}

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
